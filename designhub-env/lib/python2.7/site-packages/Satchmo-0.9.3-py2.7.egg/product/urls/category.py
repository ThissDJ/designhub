from django.conf.urls.defaults import *

urlpatterns = patterns('product.views',
    (r'^(?P<parent_slugs>([-\w]+/)*)?(?P<slug>[-\w]+)/$',
        'category_view', {'SSL': True}, 'satchmo_category'),
    (r'^$', 'category_index', {'SSL': True}, 'satchmo_category_index'),
)
