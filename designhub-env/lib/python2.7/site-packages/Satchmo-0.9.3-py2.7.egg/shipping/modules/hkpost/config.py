from django.utils.translation import ugettext_lazy as _
from livesettings import *
 
SHIP_MODULES = config_get('SHIPPING', 'MODULES')
SHIP_MODULES.add_choice(('shipping.modules.hkpost', 'HKPost'))
SHIPPING_GROUP = config_get_group('SHIPPING')
 
config_register_list(
    DecimalValue(SHIPPING_GROUP,
            'RATE_Asia',
            description=_("Rate for Asia"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_NorthAmerica',
            description=_("Rate for North America"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_SouthAmerica',
            description=_("Rate for South America"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_Africa',
            description=_("Rate for Africa"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_Europe',
            description=_("Rate for Europe"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_Oceania',
            description=_("Rate for Oceania"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),
    DecimalValue(SHIPPING_GROUP,
            'RATE_Antarctica',
            description=_("Rate for Antarctica"),
            requires=SHIP_MODULES,
            requiresvalue='shipping.modules.hkpost',
            default="14.00"),    
)
